class DepenseObj {
  var id;
  String category, montant, date, User, type;
  DepenseObj(
      {required this.category,
      required this.date,
      required this.User,
      required this.type,
      required this.montant,
      required this.id});
}
